import { BaseComponent } from '../../../../utils/base-component'
import './game-field.scss'

const content = 'game-field'

export const gameField = new BaseComponent({ tag: 'section', className: 'game-field', content })
