import { BaseComponent } from '../../../../utils/base-component'
import './panel.scss'

const content = 'Panel'

export const panel = new BaseComponent({ tag: 'section', className: 'panel', content })
